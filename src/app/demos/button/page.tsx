import CustomButton from "@/components/button";

const Page = () => {
  return (
    <div className="flex flex-col items-center justify-center px-6 mx-auto h-screen">
      <CustomButton disabled>Test</CustomButton>
    </div>
  );
};

export default Page;
